# TikTok Blur Cam – Peace Sign Effect Song by Sal Priadi
# Python >= 3.10 required (tested on Python 3.12)
#
# Install:
#   pip install -r requirements.txt

# Camera & image processing
opencv-python>=4.9.0

# Hand landmark detection
# Pinned to 0.10.14 — last stable version with solutions API (mp.solutions.hands)
mediapipe==0.10.14

# Audio playback
pygame>=2.5.2

# Numerical array operations (mediapipe dependency)
# Capped below 2.1 for stable mediapipe compatibility
numpy>=1.26.0,<2.1
